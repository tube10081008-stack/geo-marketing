# 📊 액션 효과 코호트 설계 — "우리 실측"으로 근거를 격상하는 경로

> 상태: **설계안**(미구현). 데이터 모델 변경은 **불필요** — 필요한 필드가 모두 존재한다.

## 왜 이것인가

`SCREENING.md`를 코퍼스에 적용한 결과, **현재 [M1]~[M22] 전부가 최선 `local_test_only`** 다.
원문 전문 검증 전이라 `experiment_candidate`(V≥0.7)에 구조적으로 도달할 수 없다.

그런데 문헌 평가 문서의 결론은 이랬다:

> *"좋은 논문이란 우리 고객 매장에서 실험으로 검증할 만한 가치가 있는 가설을 만들어주는 논문이다.
> 그리고 검증이 끝나면, 그 매장의 데이터가 그 어떤 논문보다 더 좋은 근거가 된다."*

**액션 효과 코호트가 그 "검증이 끝나면"을 구현한다.** 해외 논문 [M17]을
"국내 카페 55곳 실측"으로 격상시키는 유일한 경로다.

---

## 1) 무엇으로 코호트를 묶는가 — `citations`

액션 제목은 LLM이 만든 자유 텍스트라 묶을 수 없다.
"스탬프 적립제 도입" / "커피 10잔 쿠폰 시작" / "적립 카드 운영" — 같은 개입인데 문자열이 다르다.

**`Action.citations`가 구조화된 그룹 키다.** 인용 sanitizer를 도입한 뒤로
이 필드에는 **검증된 [M#]만** 남는다(환각·타담당 차용 제거됨).

```
[M17] 인용 액션들  → "적립·목표구배" 개입군
[M13] 인용 액션들  → "부정리뷰 신속대응" 개입군
[M5]  인용 액션들  → "가격 끝자리" 개입군
```

인용이 곧 **개입의 정체성**이므로, 코호트 결과가 그대로 그 근거의 국내 실측치가 된다.

---

## 2) 증거 강도 3단 — `protocol`이 가르는 지점

같은 코호트라도 설계에 따라 **허용되는 주장 강도가 다르다**(SCREENING.md §5, I축).

| 티어 | 설계 | 통제하는 것 | 허용 표현 | 추정 I |
|---|---|---|---|---|
| **A. 격주 교차(abab)** | 매장 내부에서 실행/중단 반복 | **매장 고유 특성**(입지·주인·메뉴) 전부 | "이 매장에서 반복 관찰되었다" | 0.5~0.6 |
| **B. 코호트 차분** | 실행군 vs 미실행군, 각각 업종 차분 | 계절성·시장 추세·업종 | "관련이 관찰되었다" | 0.4 |
| **C. 단순 전후** | `baseline_value → result_value` | 없음 | "변화가 있었다"(신호일 뿐) | 0.2~0.3 |

**A가 가장 값지다.** `abab_result()`가 이미 구현돼 있어 매장별 국면 평균을 낸다.
여러 매장의 abab 결과를 모으면 **준실험 다중 반복**이 되어, 관찰연구보다 훨씬 강하다.

> ⚠️ **B는 무작위 배정이 아니다.** 액션을 시작하는 매장은 애초에 더 적극적일 가능성이 높다
> (선택 편향). 업종 차분으로 계절성은 통제되지만 **동기 차이는 통제되지 않는다.**
> 따라서 B의 결과는 "효과가 있다"가 아니라 **"관련이 관찰되었다"** 까지만 말한다.

---

## 3) 이중 차분 — 이미 만든 벤치마크를 재사용

`agents/benchmark.py`가 이미 "내 증가율 vs 업종 중앙값"을 낸다. 여기에 코호트 축만 얹는다.

```
1차 차분:  각 매장의 (실행 후 - 실행 전) 증가율
2차 차분:  실행군 중앙값 − 미실행군 중앙값
                    ↑ 이것이 계절성·시장추세를 뺀 순수 신호
```

**시간축은 달력이 아니라 상대 시간**이다(코호트 표의 핵심):

```
                 시작 전   +1개월   +2개월   +3개월
[M17] 실행군 (n=32)  기준    +3.1%   +6.4%   +7.9%
     미실행군 (n=41)  기준    +1.2%   +1.8%   +2.1%
     ─────────────────────────────────────────────
     차분                    +1.9%p  +4.6%p  +5.8%p
```

미실행군의 "시작 시점"은 실행군의 시작월 분포에 맞춰 **달력 구간을 매칭**한다
(같은 계절을 보게 하기 위함).

---

## 4) 어떤 지표로 재는가

**증분 공헌이익 우선.** 액션의 `target_metric`을 쓰되, 변동비율을 아는 매장은
`unit_contribution_margin`으로 승격한다(`AdviseView._target_metric`과 동일 규칙).

매출 기준으로만 재면 **할인 액션이 마진을 깎으면서도 "성공 코호트"로 보인다.**
이건 이번에 이미 고친 함정이므로 코호트에도 같은 규율을 적용한다.

---

## 5) 정직 게이트 — 벤치마크와 동일 원칙

- **표본 n≥10** (실행군·미실행군 **각각**). 미달이면 수치를 숨기고 `n/10`만 공개
- **중앙값** 사용 (소표본에서 극단값 1건이 평균을 왜곡)
- **최소 관측 기간** 14일 (벤치마크의 `MIN_SPAN_DAYS`와 동일)
- **익명 집계만** — 개별 매장 값·상호는 절대 노출하지 않는다
- 표시할 때 **설계 티어(A/B/C)를 함께 밝힌다** — 사용자가 강도를 오해하지 않도록

---

## 6) 구현 범위 (예상)

**데이터 모델 변경 없음.** 필요한 필드가 전부 존재한다:

| 필요한 것 | 어디에 있나 | 확인 |
|---|---|---|
| 개입 그룹 키 | `Action.citations` | ✅ sanitizer 도입으로 검증된 값만 |
| 실행 시점 | `Action.started_at` | ✅ |
| 실행 여부 | `Action.status` | ✅ proposed/active/done/dropped |
| 설계 티어 | `Action.protocol` | ✅ simple/abab |
| 지표 시계열 | `KpiSnapshot` | ✅ 덮어쓰기 없이 축적 |
| 업종 차분 | `agents/benchmark.py` | ✅ 재사용 |
| 매장 내 국면 비교 | `Action.abab_result()` | ✅ 구현됨 |

**추가할 것**
- `backend/agents/cohort.py` — 코호트 구성·이중차분·게이트
- `GET /api/v1/agents/cohort/?citation=M17` — 익명 집계 반환(무과금, 순수 DB 연산)
- 프런트: 코호트 표 + 설계 티어 배지
- 테스트: 표본 게이트, 선택 편향 고지, 중앙값 방어, 상대시간 정렬, 익명성

---

## 7) ⚠️ 지금 당장은 아무 결과도 안 나온다 (정직 고지)

가입 매장이 없다. `n≥10`을 **실행군·미실행군 각각** 채우려면 같은 인용을 공유하는
액션을 시작한 매장이 **최소 20곳 이상** 필요하고, 상대시간 +1개월 칸을 채우려면
그만큼의 시간도 필요하다.

**즉 이것은 "지금 쓰는 기능"이 아니라 "지금 심어두는 자산"이다.**
초기 가입자의 액션·체크인이 쌓이는 순간부터 자동으로 채워지도록 미리 넣어두는 것이
핵심이며, 나중에 붙이면 그 사이 데이터는 영영 못 만든다(스냅샷은 소급 생성 불가).

---

## 8) 다른 두 코호트와의 관계

| 코호트 | 상태 | 비고 |
|---|---|---|
| ① 고객 리텐션 코호트 | **데이터 유입 경로 없음** | `CustomerTransaction` 모델은 완비. 프런트에 입력 UI가 없어 비어 있다. 자체 개발 중인 POS에서 API로 쏘면 열린다 |
| **② 액션 효과 코호트** | **이 문서** | 기존 인프라로 구현 가능 |
| ③ 가맹점 코호트 | 가능 | `Merchant.created_at` + 벤치마크. 가입월별 성과 추이 — 제품 개선용(고객에게 보여줄 것은 아님) |
