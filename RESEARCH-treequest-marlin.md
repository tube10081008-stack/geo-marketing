# 🔬 기술 검토: TreeQuest 이식 + "Marlin-lite for SMB"

> 요청: ① Sakana TreeQuest(AB-MCTS, Apache 2.0)를 우리 협업 라우팅에 이식 가능한가
> ② Marlin(엔터프라이즈 딥리서치, ~¥9,800/run)급 가치를 소상공인 가격으로 만들 수 있는가
> 방법: **소스 직접 클론·설치·PoC 실행**(추측 아님) + 원가 계산. PoC: `backend/agents/treequest_poc.py`

---

## Part 1 — TreeQuest 이식 검토

### 1.1 소스 감사 결과
| 항목 | 확인 내용 |
| --- | --- |
| 라이선스 | Apache 2.0 — 상용 이식 문제없음 |
| 코어 크기 | 알고리즘부 ~1,100줄 — 감사 가능한 규모, 블랙박스 아님 |
| 의존성 | **ABMCTS-A: 경량**(scipy·joblib·PyYAML·ulid). ABMCTS-M은 PyMC/JAX 스택(무거움) → **A 변형만 채택** |
| 인터페이스 | `generate(parent_state) -> (state, score∈[0,1])` 함수들의 dict = "행동들". `algo.step()` 반복. 배치용 ask-tell API 별도 제공 |
| 우리 구조와의 정합 | **행동 dict = 페르소나 dict** — 획득/전환/유지 생성기를 그대로 꽂는 1:1 매핑 |

### 1.2 PoC 실증 (오프라인·결정적, 재현 seed=42)
시나리오: 유지(ret)가 실제 최약(재방문 30%·단골 20%)인 가게. 라우터에 이 사실을 알려주지 않고 점수 피드백만 제공.
```
예산 30스텝 배분: ret 24 / acq 5 / cvr 1
최고 액션: persona=ret (score 0.806)
```
→ **AB-MCTS가 사전 규칙(임계값) 없이 최약 노드를 찾아 예산을 몰아줌.**
우리가 REVIEW §2에서 자기비판한 "휴리스틱 v1(자의적 임계값)"의 원리적 대체재가 맞다.

### 1.3 이식 설계 (어디에 쓰고, 어디에 쓰면 안 되는가)
| 위치 | 판정 | 이유 |
| --- | --- | --- |
| 일상 채팅 라우팅 | ❌ | 1스텝 = 생성 1 + 채점 1 LLM 호출. 30스텝이면 대화 한 턴에 60호출 — 비용·지연 자살 |
| **프리미엄 "월간 플랜"** | ✅ | 고부가 결정에만 추론 비용 투입(사카나의 inference-time scaling 철학). 예산=크레딧 직결 |
| Vercel 서버리스 | ❌ | 순차 30~60호출 ≈ 수십 초~수 분 — 타임아웃. **Django(Render) 비동기 잡**으로 실행 |

**운영 어댑터 스펙:**
- `generate` = Flash가 페르소나 액션 생성/정제(부모 상태를 받아 정제하는 구조가 TreeQuest와 정확히 일치)
- `score` = **LLM-judge 루브릭**(Flash-Lite): 베이스라인 적합성 / 측정가능한 목표 유무 / [M#] 인용 정합 / 실행 난이도 → 0~1

**원가 (Gemini 단가 기준):** 1스텝 ≈ 생성($0.0009) + 채점($0.0001) ≈ **$0.001** → 예산 30 ≈ **$0.03 ≈ 3크레딧**. 프리미엄 기능으로 성립.

### 1.4 정직한 리스크 — 채점기가 약한 고리
사카나의 벤치마크(코딩·ARC)는 **정답 검증기**가 있다. 마케팅 액션은 생성 시점에 정답이 없다 → LLM-judge 점수는 노이즈·편향 가능, 트리가 "심판 취향"에 과적합할 수 있음.
**완화책:** ① judge는 *사전 필터*로만 — 최종 심판은 **실측 A/B(근거등급 격상 루프)** ② 루브릭을 코퍼스 원칙으로 고정(출처 없는 단정 감점 등) ③ 후보 상위 2~3개를 사장님에게 선택지로 제시(사람이 마지막 노드).

---

## Part 2 — "Marlin-lite": 소상공인 가격의 딥 마케팅 리포트

### 2.1 Marlin이 비싼 이유와 우리의 반전
Marlin(~$65/run)의 원가 구조: **8시간 자율 탐색 × 프런티어 모델 × 범용 리서치(100페이지)**. 타깃이 금융·컨설팅이라 성립하는 가격.
우리의 반전은 "같은 걸 싸게"가 아니라 **범위 축소가 곧 가치**라는 것:
- 소상공인에게 100페이지는 반(反)가치다 — 안 읽는다.
- 가치 있는 건 **"이 가게 하나"에 대한 10~15페이지 + 실행 체크리스트**(읽는 데 10분).
- 범위가 좁으면 탐색 공간이 작고, 탐색 공간이 작으면 저가 모델 + 짧은 실행으로 충분하다.

### 2.2 파이프라인 설계 (월간 "지오 딥리포트")
```
[입력] 가게 베이스라인 + 대화이력 요약 + 코퍼스[M1~16] (+선택: 지역·업종 웹검색)
 1. 아웃라인 생성(Flash ×1)
 2. 섹션 생성 6~8개 — 핵심 2개 섹션(이달의 최약 KPI)만 AB-MCTS 예산 8~10 투입,
    나머지는 단발 생성 (Part 1 어댑터 재사용)
 3. 합성·일관성 패스(Flash ×1, 출력 6~8K 토큰)
 4. 렌더: HTML 리포트(서버) → PDF/슬라이드 요약(우리 대시보드 디자인 시스템 재사용)
[실행] Django(Render) 비동기 잡 — 5~10분 배치. POST로 잡 생성 → 폴링/알림
```

### 2.3 원가 계산 (Gemini 2.5 Flash $0.30/$2.50, Flash-Lite $0.10/$0.40 per 1M)
| 티어 | 구성 | 호출수 | 원가/런 |
| --- | --- | --- | --- |
| 라이트(플레인) | 아웃라인+섹션 단발+합성 | ~15 | **$0.03~0.05** |
| **딥(AB-MCTS 2섹션)** | + 탐색 예산 20 + 웹검색 요약 | ~55 | **$0.10~0.15** |
| (참고) Marlin | 8h 프런티어 탐색 | — | ~$65 |

→ 딥 티어 원가 **200~300원**. 판매가 **월 9,900원**이면 마진 ~97%, **Marlin 대비 1/50 가격**에 SMB 맞춤 가치.
크레딧 환산: 딥리포트 1회 ≈ 10~15크레딧(원가 상한 여유 포함) — 기존 계측=과금 체계에 그대로 얹힘.

### 2.4 기술 리스크 (정직)
| 리스크 | 평가 | 대응 |
| --- | --- | --- |
| 품질 갭(vs 프런티어 8h) | 존재함 — 우리는 깊이가 아니라 **맞춤도**로 승부 | 리포트에 "이 가게 실측 데이터" 섹션 — Marlin이 못 하는 것 |
| 웹검색 소스 신뢰 | 지역 트렌드 검색은 환각 위험 | 검색 인용 URL 필수 + 미확인 정보는 "참고" 분리(코퍼스 [M#]와 등급 구분) |
| Render 무료 티어 배치 | gunicorn 30s 타임아웃 | 요청→DB 잡 행→스레드 실행→폴링(월 1회/가게 수준이면 워커 불요) |
| 채점기 한계 | Part 1.4와 동일 | 동일 완화책 |

### 2.5 판정 및 로드맵 반영
- **TreeQuest 이식: 가능 판정** — 어댑터 코드 소량(생성기+judge), PoC로 메커니즘 검증 완료. 단 프리미엄 경로 한정.
- **Marlin-lite: 성립 판정** — 원가 1/300, 가격 1/50, 가치는 "범용 깊이→초맞춤"으로 재정의. 기존 스택(Django+Gemini+크레딧) 위에 증분 구현.
- 로드맵: **P2.5 "지오 딥리포트"** — ① judge 루브릭 확정 → ② TreeQuest 어댑터(Django 잡) → ③ 리포트 렌더러 → ④ 딥 티어 가격 실험.

### 2.6 구현 현황 (착공 완료)
`backend/agents/deepreport.py` + `DeepReport` 모델 + `POST/GET /api/v1/reports/`:
- 라이트/딥 티어, 딥은 '이달의 포커스'만 AB-MCTS(예산 8, treequest 미설치 시 best-of-3 폴백)
- judge 루브릭 v1(측정목표·[M#] 인용·구체성), 백그라운드 스레드 잡 + 폴링, 업체당 동시 1건(409)
- e2e 검증: 6섹션 생성·ab-mcts 경로·소유 격리 404·스텁 0크레딧 PASS
- 남은 것: ③ HTML/PDF 렌더러 ④ 프런트 연동(Render 배포 후) ④' 가격 실험

> ⚠️ 재확인: Fugu(OpenAI 호환 API) 세부는 2차 출처 — 엔진 대체 논의가 재점화되면 공식 문서로 재검증할 것. 본 검토의 결론(엔진 교체 ❌ / 방법 차용 ⭕)은 Fugu 가격이 절반이어도 유지된다(그래도 Gemini 대비 6~8배).
