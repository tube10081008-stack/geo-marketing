# 🔬 문헌 스크리닝 기준 — Fugu Scientific Evidence Evaluator

코퍼스([M#]) 편입 심사의 **단일 표준**. 목적은 논문의 권위 평가가 아니라,
**독립 카페의 증분 공헌이익을 안전하게 높이는 행동**을 만들 수 있는지 판단하는 것.

판정 수식·게이트는 `backend/agents/screening.py`가 **결정론적으로 재계산**한다
(스펙 지시: 모델 산술을 신뢰하지 말 것). LLM이 낸 F는 참고값일 뿐이다.

---

## 판단해야 할 5가지

1. 이 주장이 한국 독립 카페에서 검증할 가치가 있는가?
2. 어떤 수준의 표현까지 허용되는가?
3. 매장 실험으로 전환할 수 있는가?
4. **매출이 아니라 증분 공헌이익**에 기여할 가능성이 있는가?
5. 어떤 부작용과 중단 조건이 필요한가?

---

## 필수 규칙

### 1. 주장 단위 평가
논문 전체에 하나의 인과 점수를 부여하지 않는다. 핵심 주장마다 별도 평가.
개입·결과·연구설계가 다르면 분리한다.

### 2. 확인된 정보만 사용
DOI·권호·페이지·표본수·효과크기·신뢰구간·연구설계·비용효과·실험기간·규제적합성은
**추정 금지**. 확인 불가 시 `null` + `verification_notes`에 결측 기록.

### 3. 원문 검증 상태와 V 상한

| 상태 | V 상한 |
|---|---|
| `full_text_verified` | 1.0 |
| `abstract_only` | 0.6 |
| `metadata_only` | 0.3 |
| `secondary_summary_only` | 0.3 |
| `unverified` | 0.1 |

초록·2차요약만으로 세부 인과성·효과크기·부작용을 확정하지 않는다.

### 4. 연구 결과와 제품 추론 분리
- `observed_evidence` — 원 연구가 직접 보여준 것
- `transfer_inference` — 한국 독립 카페 적용을 위한 추론
- `executable_hypothesis` — 매장에서 검증할 행동 가설

원 연구에서 직접 검증되지 않았으면 **"간접 근거에 기반한 로컬 검증 가설"** 로 표시.

### 5. 인과 표현 통제
관찰연구에 "유발했다 / 높인다 / 효과가 있다" 금지.
→ "관련이 관찰되었다 / 예측 변수로 나타났다 / 함께 증감했다 / 인과관계는 확인되지 않았다"

### 6. 실행 가설 강제 생성 금지
근거 부족 시 `executable_hypothesis: null`. 실험 기간·손실 중단 수치를 근거 없이
생성하지 않는다. → "매장 기준선과 주문량을 이용해 사전 설정 필요"

### 7. 안전 및 준법 검토 (상시)
가짜 희소성·허위 마감 / 허위 사회적 증거 / 리뷰 조작·보상 미고지 / 플랫폼 약관 위반 /
개인정보·멤버십 데이터 오용 / 차별적 가격·취약 소비자 악용 / 식품 안전 저해 /
피크타임 혼잡·대기·직원 과부하 / **할인·수수료·폐기에 따른 공헌이익 훼손**

중대 위험 → `deployment_status: blocked`.

---

## 채점 루브릭 (0.0~1.0, 소수점 첫째 자리 · focal claim 단위)

### R — 관련성
점주가 다음 주에 바꿀 수 있는 행동과 가까울수록 높다.
`1.0` 가격·메뉴·세트·진열·적립·리뷰요청·영업시간 직접 변경 · `0.7` 배달앱/광고 등 추가 설정 필요 ·
`0.4` 날씨·상권 등 통제 불가하나 대응 정책 가능 · `0.1` 대기업 유통·전국 광고 · `0.0` 무관

통제 불가 예측변수는 **개입이 아니라 대응 정책 유무**로 평가한다.

### I — 내적 타당성
`0.9~1.0` 강건한 다수 현장연구·고품질 메타분석·충분한 검정력의 현장 RCT ·
`0.7~0.8` 신뢰할 단일 현장 RCT 또는 식별가정 점검된 준실험 ·
`0.5~0.6` 종단·패널 관찰(잔여 교란 가능) · `0.3~0.4` 단면 회귀·설문·자기보고 ·
`0.1~0.2` 단일 사례·전문가 의견·프레임워크 · `0.0` 주장과 데이터 불일치

설계 **이름만으로 점수 금지** — 무작위화·이탈·식별가정·신뢰구간·강건성·다중검정·결과선택 함께 평가.
유의하지 않다는 사실만으로 "효과 없음" 결론 금지 → 정밀도·동등성 검정 부족하면 **"불확실"**.

### T — 전이 가능성
하위 축: `industry_fit`(카페>음식점>소매·서비스>기타) · `geography_fit`(한국>유사시장>기타) ·
`business_size_fit`(독립>중소>대기업) · `channel_fit`(국내 현행>유사 해외>기타) · `temporal_stability`

`0.9~1.0` 한국 독립 카페 직접 · `0.7~0.8` 해외 독립카페/국내 유사 소상공인 ·
`0.4~0.6` 해외 음식점·소매 · `0.2~0.3` 대기업·상이 산업 · `0.0~0.1` 전이 근거 없음

**출판연도만으로 자동 감점 금지** — 행동 메커니즘과 플랫폼 효과의 시간 안정성은 다르다.

### A — 실행 가능성
`1.0` 개입 대상·시점·비교조건이 명확한 매장 실험으로 직접 변환 · `0.7` 일부 보정 후 가능 ·
`0.4` 방향성만 · `0.1` 추상 전략·태도 수준 · `0.0` 변환 불가

평가자가 창의적으로 행동을 만들어냈다는 이유로 고득점 금지 — **원 연구의 개입·결과가 구체적일수록** 높다.

### M — 측정 가능성
`1.0` POS·멤버십 직접 · `0.8` 배달정산·재고·폐기·인력 결합 · `0.6` 플레이스·리뷰·광고 통계 ·
`0.4` 설문·수작업 관찰 · `0.1` 태도·정서·장기 브랜드 · `0.0` 검증 불가

**중간지표를 최종 성과로 간주 금지** — 클릭·리뷰·평점은 공헌이익의 대리변수일 뿐.

### P — 공헌이익 근접성
`1.0` 증분 공헌이익·영업이익 직접 · `0.8` 매출 + 원가·할인·수수료 등 주요 변동비 ·
`0.5` 매출·구매량·주문량만 · `0.3` 클릭·리뷰·방문의도 · `0.1` 브랜드 태도·인지도 · `0.0` 연결 불가

### S — 안전·반론 가치 *(F에 미포함 · 브레이크 전용)*
`1.0` 부작용·이익훼손·역효과·무효과 직접 입증 · `0.7` 효과의 조건·이질성·지속기간 제한 입증 ·
`0.4` 실무상 주의점 제시 · `0.1` 일반 한계 문구 · `0.0` 정보 없음

> **정보가 없다는 것과 안전하다는 것은 다르다.**

### V — 검증 신뢰도
`1.0` 원문·서지·방법·결과·효과크기까지 · `0.8` 원문 확인(일부 부록 미확인) · `0.6` 초록까지 ·
`0.3` 서지/2차요약만 · `0.1` 출처 확인 불가 · `0.0` 철회·조작·허위 서지

---

## 점수 계산

```
U (제품 유용성)  = (R · T · A · M · P) ^ (1/5)
E (근거 신뢰도)  = sqrt(I · V)
F (최종 가중치)  = U · E
```

소수점 넷째 자리까지. **S는 F에 포함하지 않는다** — S가 높으면 F가 낮아도
`counter_evidence_brake`로 보존한다. U가 기하평균이므로 **한 축이라도 0에 가까우면 전체가 무너진다**(곱셈적 게이트).

---

## 역할 분류 (중복 가능)

| 역할 | 조건 |
|---|---|
| `pending_verification` | V < 0.4, 또는 원문 없이 핵심 방법·결과 판단 불가 |
| `rejected` | 철회·조작·허위 출처 / R<0.2이며 안전·반론 가치도 없음 / 주장·데이터 명백 불일치 |
| `counter_evidence_brake` | S≥0.7 · E≥0.5 · R≥0.4 |
| `experiment_candidate` | F≥0.55 · I≥0.6 · **V≥0.7** · P≥0.5 · 중대 위험 없음 |
| `hypothesis_generator` | F≥0.35 · R≥0.5 · A≥0.5 · V≥0.5 |
| `context_qualifier` | R≥0.4이며 다른 역할 미충족이나 조건·메커니즘 해석에 유용 |

**primary_role 우선순위**
`rejected` > `pending_verification` > `counter_evidence_brake` > `experiment_candidate` >
`hypothesis_generator` > `context_qualifier`

`counter_evidence_brake`는 다른 비차단 역할과 공존할 수 있다.

---

## 배치 등급

- `blocked` — 중대 준법·윤리·개인정보·플랫폼 위험, 또는 검증 불충분
- `evidence_context_only` — 설명·조건 부여에만 사용, **행동 권고 불가**
- `local_test_only` — 소규모 매장 실험 가설로만 사용
- `recommendation_eligible` — 충분히 검증됨. **점주 승인 + 매장별 경제성 확인 후** 권고

> 외부 문헌만으로 자동 실행하지 않는다. `recommendation_eligible`도 예외가 아니다.

---

## 출력 형식

YAML 코드블록 **하나만** 출력. 코드블록 밖에 서론·결론 금지.
최종 M 번호는 **코퍼스 레지스트리가 부여** — 임의 생성 금지.
`candidate_id`는 DOI 또는 정규화된 제목 기반. 확인되지 않은 값은 `null`.
핵심 주장이 여럿이면 `claim_evaluations` 배열에 각각 출력.

스키마 필드: `candidate_id` / `corpus_id` / `bibliography` / `verification` / `study_context` /
`claim_evaluations[]`(`focal_claim` · `evidence` · `scores` · `score_rationales` ·
`transfer_factors` · `calculated_scores` · `roles` · `primary_role` · `deployment_status` ·
`transfer_inference` · `claims_control` · `safety_review` · `executable_hypothesis`) /
`overall_assessment`

`claims_control.allowed_claims` / `forbidden_claims`가 **에이전트 발화 상한**을 정의한다 —
이 값이 런타임에서 페르소나 프롬프트를 제약한다.
