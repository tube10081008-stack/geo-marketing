# 마케팅 오케스트라 백엔드 (P1+계정)
Django>=5.2,<5.3
djangorestframework>=3.15
django-cors-headers>=4.0
# 에이전트 실연동(라이브). 미설치/무키면 StubProvider로 동작. Gemini는 stdlib만 사용.
anthropic>=0.40
# P2.5: 딥리포트 포커스 탐색 — AB-MCTS(경량 A-변형만 사용, RESEARCH-treequest-marlin.md)
# 미설치 환경에선 best-of-3로 자동 폴백(agents/deepreport.py)
treequest>=0.3
# 운영 배포(Render 등)
gunicorn>=22.0
psycopg[binary]>=3.1
dj-database-url>=2.1
whitenoise>=6.6
