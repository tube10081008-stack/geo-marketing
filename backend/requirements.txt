# 마케팅 오케스트라 백엔드 (P1+계정)
Django>=5.2,<5.3
djangorestframework>=3.15
django-cors-headers>=4.0
# 에이전트 실연동(라이브). 미설치/무키면 StubProvider로 동작. Gemini는 stdlib만 사용.
anthropic>=0.40
# P2.5(선택): 프리미엄 플랜 탐색 — AB-MCTS (RESEARCH-treequest-marlin.md, PoC: agents/treequest_poc.py)
# treequest>=0.1
# 운영 배포(Render 등)
gunicorn>=22.0
psycopg[binary]>=3.1
dj-database-url>=2.1
whitenoise>=6.6
